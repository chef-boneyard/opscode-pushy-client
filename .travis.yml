sudo: false
language: ruby
rvm:
  - 2.3.8
  - 2.4.5
  - 2.5.3
addons:
  apt:
    packages:
    - libzmq3-dev
cache:
  - apt
  - bundler
before_install:
  - gem install bundler
  - bundle --version
  - gem update --system
  - gem --version
branches:
  only:
    - master
